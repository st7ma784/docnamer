apiVersion: v1
kind: Service
metadata:
  name: {{ include "docnamer.fullname" . }}
  labels:
    {{- include "docnamer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "docnamer.selectorLabels" . | nindent 4 }}
